# CEntryPointLiteral.create is @Platforms(HOSTED_ONLY), so the static initializer that builds
# ENTRY has to run in the image generator; without this the analysis reaches it as runtime code
# and fails with "Method ...CEntryPointLiteral.create(...) is not available in this platform".
Args = --initialize-at-build-time=Complex
